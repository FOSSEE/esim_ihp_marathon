---
import navLinks from "./nav-links.json";
import "../styles/global.css";

import esim from "../images/webtop.png";

---
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ESIM Marathon</title>
    
        <!-- <title>Why eSim & IHP?</title> -->
        <script src="https://cdn.tailwindcss.com"></script>
        <style>
            .benefit-card {
                transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            }
            
            .benefit-card:hover {
                transform: translateY(-8px) scale(1.02);
            }
            
            .icon-container {
                background: linear-gradient(135deg, #3b82f6, #8b5cf6);
                animation: float 3s ease-in-out infinite;
            }
            
            @keyframes float {
                0%, 100% { transform: translateY(0px); }
                50% { transform: translateY(-10px); }
            }
            
            .section-header {
                background: linear-gradient(135deg, #1e40af, #7c3aed);
                -webkit-background-clip: text;
                -webkit-text-fill-color: transparent;
                background-clip: text;
            }
            
            .fade-in {
                opacity: 0;
                transform: translateY(30px);
                transition: all 0.8s ease;
            }
            
            .fade-in.visible {
                opacity: 1;
                transform: translateY(0);
            }
            
            .highlight-text {
                background: linear-gradient(120deg, #fbbf24 0%, #f59e0b 100%);
                -webkit-background-clip: text;
                -webkit-text-fill-color: transparent;
                background-clip: text;
            }
        </style>
    
  </head>


  <body class="bg-gray-50 min-h-screen">
    <!-- ✅ Responsive Navigation Bar -->
    <nav class="fixed top-0 left-0 w-full bg-indigo-600 shadow-md z-50">
      <div class="max-w-7xl mx-auto px-6 flex justify-between items-center py-4">
        
        <!-- Logo / Title -->
        <a href="/" class="text-white font-bold text-xl">ESIM Marathon</a>
  
        <!-- Desktop Menu -->
        <ul class="hidden md:flex items-center gap-10">
          <li><a href="/about" class="text-white font-semibold uppercase tracking-wide transition hover:text-yellow-300">About</a></li>
          <li><a href="/problemstatement" class="text-white font-semibold uppercase tracking-wide transition hover:text-yellow-300">Problem Statement</a></li>
          <li><a href="/resources" class="text-white font-semibold uppercase tracking-wide transition hover:text-yellow-300">Resources</a></li>
          <li><a href="/register" class="text-white font-semibold uppercase tracking-wide transition hover:text-yellow-300">Register</a></li>
          <li><a href="/#partners" class="text-white font-semibold uppercase tracking-wide transition hover:text-yellow-300">Partner</a></li>
          <li><a href="/#contact" class="text-white font-semibold uppercase tracking-wide transition hover:text-yellow-300">Contact</a></li>
        </ul>
  
        <!-- Mobile Hamburger -->
        <button id="menu-btn" class="md:hidden text-white text-3xl">&#9776;</button>
      </div>
  
      <!-- Mobile Dropdown -->
      <div id="mobile-menu" class="hidden md:hidden flex-col bg-indigo-700 px-6 py-4 space-y-3">
        <a href="/about" class="block text-white hover:text-yellow-300">About</a>
        <a href="/problemstatement" class="block text-white hover:text-yellow-300">Problem Statement</a>
        <a href="/resources" class="block text-white hover:text-yellow-300">Resources</a>
        <a href="/register" class="block text-white hover:text-yellow-300">Register</a>
        <a href="/#partners" class="block text-white hover:text-yellow-300">Partner</a>
        <a href="/#contact" class="block text-white hover:text-yellow-300">Contact</a>
      </div>
    </nav>
  
    <!-- ✅ Hero Section -->
    <!-- <section class="flex justify-center bg-white pt-20">
      <a href="https://esim.fossee.in/" target="_blank">
        <img 
          src={esim.src} 
          alt="eSim" 
          decoding="async" 
          class="w-full max-w-[1600px] h-auto mx-auto rounded-md shadow-md"
        />
      </a>
    </section>
   -->
    <!-- Simple Script for toggle -->
    <script>
      const btn = document.getElementById('menu-btn');
      const menu = document.getElementById('mobile-menu');
      btn.addEventListener('click', () => {
        menu.classList.toggle('hidden');
      });
    </script>
  </body>
    <!-- ✅ Spacer so content doesn’t hide under fixed nav -->
    <!-- <div class="pt-16"></div> -->

       <!-- ✅ Hero Section -->
  <!-- <section class="flex justify-center bg-white pt-20">
    <a href="https://esim.fossee.in/" target="_blank">
      <img 
        src={esim.src} 
        alt="eSim" 
        decoding="async" 
        class="w-full max-w-[2000px] h-auto mx-auto rounded-md shadow-md"
      />
    </a>
  </section>                             -->

    
    <main class="max-w-6xl mx-auto px-4 py-8">
      <slot />
    </main> 
  


       

          <!-- Contact Us Section -->
          <div  id="contact" class="text-center mt-16 fade-in">
              <div class="bg-gradient-to-r from-slate-700 to-gray-800 rounded-2xl p-8 text-white shadow-xl">
                  <h3 class="text-3xl font-bold mb-6">Contact Us</h3>
                  
                  <div class="flex items-center justify-center mb-6">
                      <div class="w-16 h-16 bg-white/20 rounded-full flex items-center justify-center mr-4">
                          <span class="text-3xl">📧</span>
                      </div>
                      <div class="text-left">
                          <p class="text-gray-300 text-lg mb-2">For any queries, please email:</p>
                          <p class="text-2xl font-bold text-blue-300">contact-esim[at]fossee[dot]in</p>
                      </div>
                  </div>
                  
                  <div class="bg-white/10 backdrop-blur-sm rounded-xl p-6 max-w-2xl mx-auto">
                      <p class="text-gray-200">
                          We're here to help! Don't hesitate to reach out if you have any questions about the marathon, 
                          registration process, or technical requirements.
                      </p>
                  </div>
              </div>
          </div>
      </div>
  
</main>

      
        <script>
            // Intersection Observer for fade-in animations
            document.addEventListener('DOMContentLoaded', function() {
                const observerOptions = {
                    threshold: 0.1,
                    rootMargin: '0px 0px -50px 0px'
                };
    
                const observer = new IntersectionObserver((entries) => {
                    entries.forEach(entry => {
                        if (entry.isIntersecting) {
                            entry.target.classList.add('visible');
                        }
                    });
                }, observerOptions);
    
                // Observe all fade-in elements
                document.querySelectorAll('.fade-in').forEach(el => {
                    observer.observe(el);
                });
    
                // Add click handlers for buttons
                const buttons = document.querySelectorAll('button');
                buttons.forEach(button => {
                    button.addEventListener('click', function() {
                        // Create ripple effect
                        const ripple = document.createElement('span');
                        ripple.className = 'absolute inset-0 rounded-lg bg-white/20 transform scale-0 animate-ping';
                        this.style.position = 'relative';
                        this.appendChild(ripple);
                        
                        setTimeout(() => {
                            ripple.remove();
                        }, 600);
                    });
                });
            });
               // Intersection Observer for fade-in animations
               document.addEventListener('DOMContentLoaded', function() {
                   const observerOptions = {
                       threshold: 0.1,
                       rootMargin: '0px 0px -50px 0px'
                   };
       
                   const observer = new IntersectionObserver((entries) => {
                       entries.forEach(entry => {
                           if (entry.isIntersecting) {
                               entry.target.classList.add('visible');
                           }
                       });
                   }, observerOptions);
       
                   // Observe all fade-in elements
                   document.querySelectorAll('.fade-in').forEach(el => {
                       observer.observe(el);
                   });
       
                   // Add click handlers for buttons
                   const buttons = document.querySelectorAll('button');
                   buttons.forEach(button => {
                       button.addEventListener('click', function() {
                           // Create ripple effect
                           const ripple = document.createElement('span');
                           ripple.className = 'absolute inset-0 rounded-lg bg-white/20 transform scale-0 animate-ping';
                           this.style.position = 'relative';
                           this.appendChild(ripple);
                           
                           setTimeout(() => {
                               ripple.remove();
                           }, 600);
                       });
                   });
               });
           </script>
        </body> 
       </html>
       