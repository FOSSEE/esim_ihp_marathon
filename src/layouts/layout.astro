---
import "../styles/global.css";

---
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>eSim Marathon</title>
    <link rel="icon" type="image/png" href="/esim_logo.png" />
    <link
  rel="stylesheet"
  href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
/>



        <!-- <title>Why eSim & IHP?</title> -->
        <script src="https://cdn.tailwindcss.com"></script>
        <style>
            .benefit-card {
                transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            }
            
            .benefit-card:hover {
                transform: translateY(-8px) scale(1.02);
            }
            
            .icon-container {
                background: linear-gradient(135deg, #3b82f6, #8b5cf6);
                animation: float 3s ease-in-out infinite;
            }
            
            @keyframes float {
                0%, 100% { transform: translateY(0px); }
                50% { transform: translateY(-10px); }
            }
            
            .section-header {
                background: linear-gradient(135deg, #1e40af, #7c3aed);
                -webkit-background-clip: text;
                -webkit-text-fill-color: transparent;
                background-clip: text;
            }
            
            .fade-in {
                opacity: 0;
                transform: translateY(30px);
                transition: all 0.8s ease;
            }
            
            .fade-in.visible {
                opacity: 1;
                transform: translateY(0);
            }
            
            .highlight-text {
                background: linear-gradient(120deg, #fbbf24 0%, #f59e0b 100%);
                -webkit-background-clip: text;
                -webkit-text-fill-color: transparent;
                background-clip: text;
            }
        </style>
    
  </head>


  <body class="bg-gray-50 min-h-screen">
    <!-- ✅ Responsive Navigation Bar -->
    <nav class="fixed top-0 left-0 w-full bg-indigo-600 shadow-md z-50">
      <div class="max-w-7xl mx-auto px-6 flex justify-between items-center py-4">
        
        <!-- Logo / Title -->
        <!-- <a href="" class="text-white font-bold text-xl">eSim Marathon</a> -->
        <a href="/" class="flex items-center space-x-2">
            <!-- Logo -->
            <img src="/esim_logo.png" alt="eSim Logo" class="h-8 w-8" />
            <!-- Title -->
            <span class="text-white font-bold text-xl">eSim Marathon</span>
          </a>
          
  
        <!-- Desktop Menu -->
        <ul class="hidden md:flex items-center gap-10">
          <li><a href="/#about" class="text-white text-xl font-bold  tracking-wide transition hover:text-yellow-300">About</a></li>
          <li><a href="/#timeline" class="text-white text-xl font-bold  tracking-wide transition hover:text-yellow-300">Timeline</a></li>

          <li><a href="/problemstatement" class="text-white text-xl font-bold  tracking-wide transition hover:text-yellow-300">Problem Statement</a></li>
          <li><a href="/resources" class="text-white font-bold   text-xl tracking-wide transition hover:text-yellow-300">Resources</a></li>

          <li><a href="/register" class="text-white font-bold text-xl  tracking-wide transition hover:text-yellow-300">Register</a></li>
          <!-- <li><a href="/#register" class="text-white font-semibold  tracking-wide transition hover:text-yellow-300">Rules</a></li> -->

                    <li><a href="/webinar" class="text-white font-bold text-xl  tracking-wide transition hover:text-yellow-300">Webinar</a></li>

          <li><a href="/#partners" class="text-white font-bold  text-xl tracking-wide transition hover:text-yellow-300">Partners</a></li>
          <li><a href="/#contact" class="text-white font-bold text-xl tracking-wide transition hover:text-yellow-300">Contact</a></li>
        </ul>
  
        <!-- Mobile Hamburger -->
        <button id="menu-btn" class="md:hidden text-white text-3xl">&#9776;</button>
      </div>
  
      <!-- Mobile Dropdown -->
      <div id="mobile-menu" class="hidden md:hidden flex-col bg-indigo-700 px-6 py-4 space-y-3">
        <a href="/about" class="block text-white hover:text-yellow-300">About</a>
        <a href="/problemstatement" class="block text-white hover:text-yellow-300">Problem Statement</a>
        <a href="/resources" class="block text-white hover:text-yellow-300">Resources</a>
        <a href="/register" class="block text-white hover:text-yellow-300">Register</a>
                <a href="/webinar" class="block text-white hover:text-yellow-300">Webinar</a>

        <a href="/#partners" class="block text-white hover:text-yellow-300">Partner</a>
        <a href="/#contact" class="block text-white hover:text-yellow-300">Contact</a>
      </div>
    </nav>
  
    <!-- ✅ Hero Section -->
    <!-- <section class="flex justify-center bg-white pt-20">
      <a href="https://esim.fossee.in/" target="_blank">
        <img 
          src={esim.src} 
          alt="eSim" 
          decoding="async" 
          class="w-full max-w-[1600px] h-auto mx-auto rounded-md shadow-md"
        />
      </a>
    </section>
   -->
    <!-- Simple Script for toggle -->
    <script>
      const btn = document.getElementById('menu-btn');
      const menu = document.getElementById('mobile-menu');
      btn.addEventListener('click', () => {
        menu.classList.toggle('hidden');
      });
    </script>
  </body>
    <!-- ✅ Spacer so content doesn’t hide under fixed nav -->
    <!-- <div class="pt-16"></div> -->

       <!-- ✅ Hero Section -->
  <!-- <section class="flex justify-center bg-white pt-20">
    <a href="https://esim.fossee.in/" target="_blank">
      <img 
        src={esim.src} 
        alt="eSim" 
        decoding="async" 
        class="w-full max-w-[2000px] h-auto mx-auto rounded-md shadow-md"
      />
    </a>
  </section>                             -->

    
    <main class="max-w-6xl mx-auto px-4 py-8">
      <slot />
    </main> 
  


       

      <footer class="bg-gray-900 text-gray-300 py-10 mt-16">
        <div class="max-w-7xl mx-auto px-6 grid grid-cols-1 md:grid-cols-4 gap-8 border-t border-gray-700 pt-8">
          
          <!-- Social Section -->
          <div>
            <h3 class="text-white font-semibold mb-4">Follow us:</h3>
            <div class="flex space-x-4">
              <a href="https://www.facebook.com/FOSSEENMEICT/" target="_blank" class="hover:text-white">
                <i class="fab fa-facebook text-xl"></i>
              </a>
              <a href="https://twitter.com/FOSSEE/" target="_blank" class="hover:text-white">
                <i class="fab fa-twitter text-xl"></i>
              </a>
              <a href="https://www.instagram.com/fossee_nmeict/" target="_blank" class="hover:text-white">
                <i class="fab fa-instagram text-xl"></i>
              </a>
              <a href="https://www.linkedin.com/company/fossee-iitb/" target="_blank" class="hover:text-white">
                <i class="fab fa-linkedin text-xl"></i>
              </a>
            </div>
          </div>
      
          <!-- About Section -->
          <div>
            <h3 class="text-white font-semibold mb-4">About</h3>
            <ul class="space-y-2">
              <li><a href="https://fossee.in/" class="hover:text-white">FOSSEE</a></li>
            </ul>
          </div>
      
          <!-- FOSSEE Section -->
          <div>
            <h3 class="text-white font-semibold mb-4">FOSSEE</h3>
            <ul class="space-y-2">
              <li><a href="https://esim.fossee.in" target="_blank" class="hover:text-white">esim.fossee.in</a></li>
            </ul>
          </div>

          <div class="mt-4 md:mt-0">
            <h3 class="font-semibold">Contact us:</h3>
            <a href="mailto:contact-esim@fossee.in" class="hover:text-white">contact-esim[at]fossee[dot]in</a>
          </div>
        
      
          <!-- Resources Section -->
          <!-- <div>
            <h3 class="text-white font-semibold mb-4">Resources</h3>
            <ul class="space-y-2">
              <li><a href="https://esim.fossee.org" target="_blank" class="hover:text-white">esim.fossee.org</a></li>
            </ul>
          </div>
        </div> -->
      
 <!-- Bottom Section -->
 <div class="max-w-3xl mx-auto px-6 mt-8 flex flex-col md:flex-row justify-between items-center text-sm">
  
  <div>
    <p class="mb-2 md:mb-0">Creative Commons License</p>
    <p>This work is licensed under a Creative Commons Attribution-ShareAlike 4.0 International License.</p>
  </div>
</div>        
</main>

      
        <script>
            // Intersection Observer for fade-in animations
            document.addEventListener('DOMContentLoaded', function() {
                const observerOptions = {
                    threshold: 0.1,
                    rootMargin: '0px 0px -50px 0px'
                };
    
                const observer = new IntersectionObserver((entries) => {
                    entries.forEach(entry => {
                        if (entry.isIntersecting) {
                            entry.target.classList.add('visible');
                        }
                    });
                }, observerOptions);
    
                // Observe all fade-in elements
                document.querySelectorAll('.fade-in').forEach(el => {
                    observer.observe(el);
                });
    
                // Add click handlers for buttons
                const buttons = document.querySelectorAll('button');
                buttons.forEach(button => {
                    button.addEventListener('click', function() {
                        // Create ripple effect
                        const ripple = document.createElement('span');
                        ripple.className = 'absolute inset-0 rounded-lg bg-white/20 transform scale-0 animate-ping';
                        this.style.position = 'relative';
                        this.appendChild(ripple);
                        
                        setTimeout(() => {
                            ripple.remove();
                        }, 600);
                    });
                });
            });
               // Intersection Observer for fade-in animations
               document.addEventListener('DOMContentLoaded', function() {
                   const observerOptions = {
                       threshold: 0.1,
                       rootMargin: '0px 0px -50px 0px'
                   };
       
                   const observer = new IntersectionObserver((entries) => {
                       entries.forEach(entry => {
                           if (entry.isIntersecting) {
                               entry.target.classList.add('visible');
                           }
                       });
                   }, observerOptions);
       
                   // Observe all fade-in elements
                   document.querySelectorAll('.fade-in').forEach(el => {
                       observer.observe(el);
                   });
       
                   // Add click handlers for buttons
                   const buttons = document.querySelectorAll('button');
                   buttons.forEach(button => {
                       button.addEventListener('click', function() {
                           // Create ripple effect
                           const ripple = document.createElement('span');
                           ripple.className = 'absolute inset-0 rounded-lg bg-white/20 transform scale-0 animate-ping';
                           this.style.position = 'relative';
                           this.appendChild(ripple);
                           
                           setTimeout(() => {
                               ripple.remove();
                           }, 600);
                       });
                   });
               });
           </script>
        </body> 
       </html>
       